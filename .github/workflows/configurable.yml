name: Configurable runner

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ${{ vars.RUNNER_LINUX || 'ubuntu-latest' }}

    steps:
    - name: Echo
      run: ${{ vars.RUNNER_LINUX || 'ubuntu-latest' }}
