name: JUnit Flaky detection

on:
  push:
  pull_request:

jobs:
  define-matrix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: 11
      - run:
          mvn install -Dsurefire.rerunFailingTestsCount=100 -f junit-testsuite-poc/pom.xml